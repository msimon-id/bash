# 1. Alles ignorieren
*

# 2. Alle Verzeichnisse wieder erlauben (sonst wird nicht rekursiert)
!*/

# 3. Gezielt Dateitypen/Dateien whitelisten
!*.sh
!*.py
!README.md
!.gitignore

# 4. Verzeichnisse, die trotzdem komplett raus sollen (z.B. Build-Output)
node_modules/
venv/
*.log
